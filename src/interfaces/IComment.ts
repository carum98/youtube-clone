interface IComment {
    id: string
    text: string
    authorDisplayName: string
    authorProfileImageUrl: string
    likeCount: number
    publishedAt: string
    authorChannelUrl: string
}

export default IComment
